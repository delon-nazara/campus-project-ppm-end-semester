<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Gradient Background -->
    <gradient
        android:startColor="#FFFFFF"
        android:centerColor="#F6EBE7"
        android:endColor="#F5E1DB"
        android:angle="90"
        android:centerX="0.46"
        android:type="linear" />
    <!-- Border (Stroke) -->
    <stroke
        android:width="1dp"
        android:color="#FF000000" /> <!-- Warna border -->

    <!-- Rounded Corners -->
    <corners android:radius="32dp" />
</shape>